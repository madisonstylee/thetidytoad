specVersion: "v1"
serviceId: "web-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "web-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
